const CourseViewer=()=>{
//should get values from db and put it in a loop to show course name and description
    return(<>
    <select>
        <option value="{}">{}</option>
    </select>
    </>)
}
export default CourseViewer;